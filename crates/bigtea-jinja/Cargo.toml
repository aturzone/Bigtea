[package]
name = "bigtea-jinja"
description = "The Jinja subset GGUF chat templates actually use, and a refusal for everything else."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

# No dependencies, like `bigtea-grammar`. A template engine that pulled in a
# general-purpose Jinja crate would accept far more than the subset below, and
# accepting more is the failure mode here rather than the goal -- see the crate
# docs on why a construct this build does not understand must be REFUSED.
[dependencies]
