I have a TypeScript React application where a table of ten thousand rows re-renders on every keystroke in a search box. Walk me through diagnosing this. Show me how to use the profiler to find which components re-render, explain the difference between memoizing the row component and memoizing the filtered array, and write the debounced search hook with correct cleanup so a pending timer does not fire after unmount. Then explain why passing an inline arrow function as a prop defeats memoization, and show the fix using useCallback with the correct dependency array. Finally, describe when virtualisation becomes the right answer instead of memoization, and what the accessibility cost of a virtualised table is for screen readers.
